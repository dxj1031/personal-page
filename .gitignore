node_modules/
*.log
*.local.md

# graphify knowledge graph — regenerate with `graphify --update`, do not ship
graphify-out/

# frames the dev server writes so the agent can see what it rendered
**/.claude/shot.png
**/.claude/zine-final.png

# personal hook and permission overrides, not a repo convention
**/.claude/settings.local.json
